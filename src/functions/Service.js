import { validateData } from './Utilities';
    
    // ...get data from somewhere and then validate it...
    // const errors = validateData(data)
    // if (errors.length) {
      // return errors || throw exception with errors || reconcile invalid data based on error types
    // }
    
    // data passed validation; proceed with business logic

  